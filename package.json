{
  "name": "softbiscuit",
  "version": "0.0.1",
  "private": true,
  "description": "Cross-platform self-hosted NVR with camera-agnostic ingest and on-device AI",
  "license": "MIT",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "fetch-vendor": "tsx scripts/fetch-vendor.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
